using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InfectedDog : MonoBehaviour , RunningMonster
{
    public void Move() { Debug.Log("InfectedDog Move"); }
    public void Attack() { Debug.Log("InfectedDog Attack"); }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
